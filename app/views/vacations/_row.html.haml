%table.table.table-striped.table-hover#vacations
  %thead
    %th
    %th User
    %th Role
    %th Starts at
    %th Ends at
    %th

  %tbody
    - @users.each do |user|
      - if user.vacations.present?
        - user.vacations.each do |vacation|
          %tr
            %td= user.gravatar_image(size: 40)
            %td
              = profile_link user
              .icons
                = mail_to user.email do
                  = tooltip user.email do
                    = icon 'envelope-o'
                = tooltip user.skype do
                  = user.skype_link()
            %td= user.role
            %td= vacation.starts_at.to_date
            %td= vacation.ends_at.to_date
            %td
              - if current_user.admin? || current_user == user
                = link_to "Edit", edit_user_vacation_path(user, vacation), method: :get, class: "btn btn-xs btn-default"
                = link_to "Delete", user_vacation_path(user, vacation), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-xs btn-danger"
